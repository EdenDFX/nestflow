import type { ReactNode } from "react";

/** Admin route styles are loaded via AdminMainShell for the full admin suite. */
export default function AdminLayout({ children }: { children: ReactNode }) {
  return children;
}
